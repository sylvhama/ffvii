---
import clsx from "clsx";

interface Props {
  href: string;
  classname?: string;
}

const { href, classname, ...restProps } = Astro.props;
---

<a
  class={clsx(
    "pointer font-mono",
    href &&
      "text-purple hover:text-pink hover:underline focus:text-pink dark:text-blue dark:hover:text-pink dark:focus:text-pink ff7:text-cyan ff7:hover:text-ff7Pink",
    href && "hover:underline focus-visible:underline",
    classname
  )}
  href={href}
  {...restProps}><slot /></a
>
