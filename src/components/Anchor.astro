---
import clsx from "clsx";

interface Props {
  href: string;
  classname?: string;
}

const { href, classname, ...restProps } = Astro.props;
---

<a
  class={clsx(
    "pointer",
    href && "text-blue-600 dark:text-blue-500",
    href && "hover:underline focus-visible:underline",
    classname
  )}
  href={href}
  {...restProps}><slot /></a
>
